日志模块（timber+Logger+LogUtils）

{在Logcat中完美的格式化输出，再也不用担心和手机其他APP或者系统的日志信
息相混淆了
包含线程、类、方法信息，可以清楚地看到日志记录的调用堆栈
支持跳转到源码处
支持格式化输出JSON、XML格式信息}
{
但并不支持诸如List、Set、Map和数组等常见Java集合类的格式化输出。
}
{它的特点是可扩展的框架，开发者可以方便快捷的集成不同类型的日志记录方
式，
}

JSON解析能力（Fastjson.android 256KB）

{它采用一种“假定有序快速匹配”的算法，把JSON Parse的性能提升到极致，
号称是目前Java语言中最快的JSON库。Fastjson接口简单易用，已经被广泛使用
在缓存序列化、协议交互、Web输出、Android客户端等多种应用场景。}

数据库 （greenDAO 100KB）

{
是一个轻量级且快速的ORM框架，专门为Android高度优化和定制，它能够支持每
秒数千条记录的CRUD操作。}

网络请求 （okhttp）{在网络性能很差的情况下能够很好地工作，它能够避免常
见的网络连接问题。}

图片加载（Picasso）